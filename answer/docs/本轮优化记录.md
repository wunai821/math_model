# 2026-09-11 本轮优化记录

## 问题4

按原有七层字典序目标比较后，正式方案从 `(4,0,1,135,16,37,826)` 更新为 `(3,0,3,141,18,37,848)`，有效执行计划从146项增至147项。A类全部保留；B类撤销从1增至3，调整总数从135增至141。这是总撤销数优先顺序下的改善。

| 实验 | 求解时间 | 结果 | 可作出的结论 |
|---|---:|---|---|
| 全模型撤销不超过3 | 51.47秒 | 找到3撤销方案 | 3撤销可行 |
| 全模型撤销不超过2 | 120.02秒 | UNKNOWN | 不能判定2撤销是否可行 |
| 撤销不超过3且A类全保 | 83.55秒 | 找到3撤销方案 | 3撤销与A类全保可同时实现 |

以上为可行性检验；无优化目标的求解器返回OPTIMAL只表示找到满足约束的方案，不表示撤销总数最优。已知全局下界2来自此前等价模型，故目前最优撤销数在2至3之间。后续固定撤销总数3，按原目标顺序继续优化，得到正式方案。

新方案仍采用无统一结束时刻上限的模型。超过643的有效计划是C068（650）、C088（651）、C082（652）、C085（681）。问题3保持原固定基准和窗口，新增142台的结论不变。

## 问题2

固定已证明的总撤销6、A类撤销0，额外要求B类撤销不超过3。170.02秒、2线程搜索返回UNKNOWN，未产生满足该上限的新方案，不能据此证明B类撤销4最优。正式方案和问题3基准保持不变。实验输出中的旧方案仅作回退基准，不满足新加的B类上限。

## 交付核验

问题4正式方案通过独立单参数、幅度和全部周期冲突检查；147项有效计划共检查10731对，冲突数为0。提交表逐字段核验通过，并同步生成问题4报告及相关图表。问题3重新核验通过，新增142项与144项固定计划之间不存在冲突。

实验记录位于 `answer/.cache/q4/cancel_limit_*_check.json`；正式方案位于 `answer/.cache/q4/solution.json`，历史4撤销方案保留在 `before_cancel3_20260911.json`。
