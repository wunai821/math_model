# D题：时频冲突检测与消解建模思路与解法分析

本项目采用 **确定性几何求交 + 离散时频约束满足与多目标整数规划（OR-Tools CP-SAT）**。问题1负责“时频交叠检测与冲突图构建”，问题2负责“多目标层次化参数优化与时频冲突消解”。

---

## 一、 题目背景与核心规则梳理

1. **时频资源离散化**：
   - **时域**：以固定的时间长度 $\Delta t$ 为离散单位，所有用频时间必须为 $\Delta t$ 的整数倍。
   - **频域**：以固定带宽 $\Delta f$ 将系统总带宽划分为 100 个等宽频段，索引范围为 $[0, 100)$。装备占用的连续频段数量（频宽）保持不变。
2. **计划的脉冲式周期使用**：
   - 设装备 $i$ 的频段区间为 $[a_i, b_i)$，首次使用时段为 $[s_i, e_i)$，单次时长 $d_i = e_i - s_i$，空闲间隔为 $g_i$，使用次数为 $n_i$。
   - 装备的第 $k$ 次使用时段展开为：
     $$\tau_{i,k} = [s_i + k(d_i + g_i),\, e_i + k(d_i + g_i)), \quad k = 0, 1, \dots, n_i - 1$$
3. **消解动作与限制**：
   - **单参数调整原则**：只能调整一个参数（平移频段区间 或 平移时间区间）或撤销计划，三者互斥；
   - **平移幅度限制**：频段平移量 $u_i \in [-10, 10]\Delta f$，时间平移量 $v_i \in [-5, 5]\Delta t$；
   - **物理边界与不可变项**：$0 \le a_i + u_i < b_i + u_i \le 100$，$s_i + v_i \ge 0$；频宽、单次时长、间隔时长、使用次数均不可变；
   - **优先级准则**：$A > B > C$；尽量少撤销、尽量少调整、尽量减小调整幅度。

---

## 二、 问题 1：时频冲突检测建模与算法

### 1. 数学判据
两台装备 $i$ 与 $j$ 发生时频冲突，当且仅当**频域与时域同时存在交叠**。全部区间统一采用**左闭右开 $[ \cdot, \cdot )$** 形式，端点相接合法（不计为冲突）：
1. **频段交叠**：
   $$\max(a_i, a_j) < \min(b_i, b_j)$$
2. **时段交叠**：存在 $k \in \{0, \dots, n_i - 1\}$ 与 $l \in \{0, \dots, n_j - 1\}$，使得：
   $$\max(s_i + k(d_i+g_i),\, s_j + l(d_j+g_j)) < \min(e_i + k(d_i+g_i),\, e_j + l(d_j+g_j))$$

两条件同时满足时，将无序装备对 $\{i, j\}$ 记录为一条冲突边。

### 2. 算法实现与双重交叉验证
- **主遍历算法（`solve.py`）**：
  - 遍历全部 $\binom{150}{2} = 11175$ 个装备对；
  - **两级剪枝**：先通过 $O(1)$ 的频段交叠检验，若不相交则直接跳过；若频段相交，再循环比较 $n_i \times n_j$ 个周期时段；
  - 无论重叠多少个周期时段，装备对 $\{i, j\}$ 仅输出一次，总时间复杂度为 $O(N^2 \cdot K^2)$（数十毫秒内完成）。
- **独立离散网格复核（Cross-Validation）**：
  - 构建网格占用映射字典 `occupants[(t, f)] -> [equipment_ids]`；
  - 提取所有共用同一单位网格的装备组合并去重；
  - **复核结论**：区间判定法与网格占用法所得冲突对集合一致。

### 3. 统计结果与冲突网络特征
- **受冲突影响规模**：全部 150 项计划中，**148 项**存在冲突（占比 $98.67\%$），仅 **C007、C060** 原生无冲突；
- **冲突对总数**：共计 **297 对**冲突装备（若按周期时段重叠组合展开，则共有 431 次交叠事件）；
- **冲突类别分布**：
  | 冲突类型 | 冲突对数 | 占比 | 特征说明 |
  | :--- | :---: | :---: | :--- |
  | **A–A** | **0** | **0.00%** | A 类内部无冲突 |
  | **A–B** | 21 | 7.07% | 高、中优先级间的冲突 |
  | **A–C** | 66 | 22.22% | 高、低优先级间的冲突 |
  | **B–B** | 10 | 3.37% | B 类内部冲突较少 |
  | **B–C** | **181** | **60.94%** | 各类别中冲突对数最多 |
  | **C–C** | 19 | 6.40% | C 类内部冲突 |
  | **合计** | **297** | **100.00%** | 占全网可能装备对的 $2.66\%$ |
- **最大冲突度**：节点度数最大为 8（B009、B016、B024、B027、B030、C076 各与 8 台其他装备冲突）。

---

## 三、 问题 2：时频冲突消解建模与求解

### 1. 变量设置与单参数互斥
对每台装备 $i$ 引入四组核心决策变量：
- $u_i \in [-10, 10] \cap \mathbb{Z}$：频率平移量；
- $v_i \in [-5, 5] \cap \mathbb{Z}$：时间平移量；
- $z_i \in \{0, 1\}$：撤销指示变量（1 表示撤销，0 表示保留）；
- $c_i \in \{0, 1\}$：是否发生调整。

**硬约束体系**：
1. **单参数互斥**：$u_i$ 与 $v_i$ 至多一个非零；若撤销（$z_i = 1$），则 $u_i = 0, v_i = 0, c_i = 0$；
2. **频域边界**：$0 \le a_i + u_i$ 且 $b_i + u_i \le 100$；
3. **时域非负**：$s_i + v_i \ge 0$。

### 2. 紧凑相对位移模型（`solve_compact.py`）
若在 CP-SAT 模型内显式对所有单位时频单元构建互斥约束，资源约束多达数万条。项目设计了**基于相对平移差预计算的紧凑模型**：
- **模型关系**：在仅允许首次时间平移时，装备 $i$ 与 $j$ 的周期时段是否冲突由相对时间平移量 $\delta = v_i - v_j$ 确定。
- **离线安全差集预计算**：
  由于 $v_i, v_j \in [-5, 5]$，相对差值 $\delta \in [-10, 10]$ 仅有 21 种可能。程序离线遍历计算出所有能使 $i$ 与 $j$ 的全部使用时段不相交的合法相对差集合 $S_{ij} \subseteq \{-10, \dots, 10\}$。
- **冲突消解逻辑约束**：
  任意两台装备 $i, j$ 只要满足以下任一条件，即可保证不冲突：
  $$z_i = 1 \quad \lor \quad z_j = 1 \quad \lor \quad (b_i + u_i \le a_j + u_j) \quad \lor \quad (b_j + u_j \le a_i + u_i) \quad \lor \quad ((v_i - v_j) \in S_{ij})$$
- **模型规模**：全网构建 **2142 组装备对约束**，相较显式网格约束减少约束数量。问题2的最小撤销数为 6，且该目标阶段返回 OPTIMAL。

### 3. 参考对比模型：有限候选网格模型（`solve.py`）
- 为每台装备离散列出所有合法动作（1 原样 + 20 频移 + 10 时移 + 1 撤销，剔除越界项后全网共 4589 个候选动作）；
- 引入 0-1 变量选择候选，建立 35406 条离散时频网格互斥约束；
- 该模型可作为紧凑模型的对照组，并用于为紧凑模型提供高质量的热启动提示（Warm-up Hints）。

### 4. 分层字典序多目标优化（Lexicographical Optimization）
针对题意给出的优先级与多重定性目标，采用严格的分层字典序逐级优化，每阶段固定前序目标最优/可行值后继续搜索下一目标：

$$\begin{aligned}
\text{阶段 1: } & \min \sum z_i && \text{（撤销总数最小）} \\
\text{阶段 2: } & \min \sum_{i \in A} z_i && \text{（A类撤销数最小）} \\
\text{阶段 3: } & \min \sum_{i \in B} z_i && \text{（B类撤销数最小）} \\
\text{阶段 4: } & \min \sum c_i && \text{（调整总数最小）} \\
\text{阶段 5: } & \min \sum_{i \in A} c_i && \text{（A类调整数最小）} \\
\text{阶段 6: } & \min \sum_{i \in B} c_i && \text{（B类调整数最小）} \\
\text{阶段 7: } & \min \sum (|u_i| + 2|v_i|) && \text{（归一化平移总成本最小）}
\end{aligned}$$

> **量纲归一化说明**：最大频移为 10，最大时移为 5。采用 $|u_i| + 2|v_i|$ 等价于归一化幅度之和 $\sum \left(\frac{|u_i|}{10} + \frac{|v_i|}{5}\right) \times 10$，避免了直接相加不同物理量纲带来的偏差。

### 5. 问题 2 当前求解状态与指标（表 1 统计）
- **问题 2 当前执行方案**：
  | 类别 | 原样保留数量 | 调整数量 | 撤销数量 | 最终执行数 |
  | :--- | :---: | :---: | :---: | :---: |
  | **A 类** | 4 | 16 | **0** | 20 |
  | **B 类** | 2 | 34 | **4** | 36 |
  | **C 类** | 12 | 76 | **2** | 88 |
  | **合计** | **18** | **126** | **6** | **144** |

- **调整细分**：频段平移 91 项，时间平移 35 项。
- **撤销装备清单**：B009、B018、B024、B028、C027、C054。
- **各阶段目标与求解器界限**：
  | 目标 | 当前方案值 | 求解器证明下界 | 求解状态 | 耗时 |
  | :--- | :---: | :---: | :---: | :---: |
  | 撤销总数 | **6** | **6.0** | **OPTIMAL（已证最优）** | 123.9s |
  | A类撤销数 | **0** | **0.0** | **OPTIMAL（已证最优）** | 6.6s |
  | B类撤销数 | 4 | 1.0 | FEASIBLE | 480.0s |
  | 调整总数 | 126 | 55.0 | FEASIBLE | 120.1s |
  | A类调整数 | 16 | 8.0 | FEASIBLE | 60.0s |
  | B类调整数 | 34 | 20.0 | FEASIBLE | 60.0s |
  | 归一化平移成本 | 775 | 12.0 | FEASIBLE | 60.0s |

### 6. 问题 2 独立验证闭环（`verify.py`）
不依赖求解器内部逻辑，通过外部独立程序重新核查：
1. 每台装备的操作满足“单参数调整、幅度受限、频段 $[0,100]$ 内、时间非负、时长间隔次数不变”；
2. 对问题 2 最终执行的 144 项计划重新做 $O(N^2)$ 全周期时频碰撞检测，**残余冲突严格为 0**；
3. 严格核对问题 2 的 `result2.xlsx` 导出的行列规范，未填项留空，无多余标记。

---

## 四、 后续问题与结果边界

### 1. 问题2的历史续算与当前状态

#### （1）B 类与 C 类的撤销权衡（历史记录）
- **历史现象**：此前方案撤销了 **5 台 B 类、1 台 C 类**；本轮续算已将 B 类撤销降至4台，C类撤销增至2台。
- **分析**：题面明确指出：“A类最高，B类其次，C类最低，高优先级用频装备的用频计划应尽量保持”。本轮 `cancel_B` 阶段耗时480秒，返回 `FEASIBLE`，当前值为4，求解器下界为1，尚未证明B类撤销数最优。

#### （2）调整总数与原样保留数的变化
- 历史续算前方案调整了125项（原样保留19项）；当前正式方案调整126项（原样保留18项），阶段4本轮下界为55项；
- 后续可在固定前序目标的条件下继续搜索更低的调整总数。

### 2. 问题3的固定基准与窗口

问题3固定问题2的当前方案作为基准，不重新优化问题2；基准包含 144 项有效计划。新增 C 类装备在频域 $[0,100)$、时域 $[0,643)$ 内排布，其中时间窗口右端点 643 取自附件1原始计划最后一次使用结束时刻的最大值，右端点采用半开区间约定。新增装备的需求固定为频宽 3、单次时长 2、间隔 8、使用 12 次。在该固定基准和窗口假设下，最多新增 **142 台**，CP-SAT 返回 OPTIMAL，目标上界同为 142。

### 3. 问题4的时间边界与正式结果

问题4从附件1原始计划重新求解，允许 C 类调整使用间隔；题目没有给出统一的结束时刻上限，因此当前模型只施加非负时间和题目规定的调整幅度约束。历史方案曾达到 **(4, 0, 1, 135, 16, 37, 826)**。当前择优方案的目标向量为 **(3, 0, 3, 141, 18, 37, 848)**，撤销总数为 3，A 类撤销数为 0，B 类撤销数为 3，调整总数为 141，独立验证冲突数为 0；撤销总数已知最强下界为 2，当前方案及后续目标均为限时可行结果，尚未证明字典序最优。相较历史4撤销方案，撤销总数按既定优先顺序改善，A 类撤销数保持为0；代价是 B 类撤销由1增至3、调整总数由135增至141。

问题3的 $[0,643)$ 窗口不能套用于问题4。按当前 `answer/.cache/q4/solution.json` 计算，问题4有 4 台有效计划的最后结束时刻超过 643，分别为 C068（650）、C088（651）、C082（652）、C085（681）；其中最晚为 681。若为问题4另行规定统一窗口，必须把该上限同时加入模型和验证器后重新求解，不能将两个问题的结果混用。
